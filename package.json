{
  "name": "bible",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "predev": "node scripts/generate-cache-manifest.mjs",
    "build": "node scripts/generate-cache-manifest.mjs && vite build && node -e \"require('fs').copyFileSync('dist/index.html','dist/404.html')\"",
    "build:manifest": "node scripts/generate-cache-manifest.mjs",
    "build:versions": "python scripts/build-version-chapters.py",
    "build:cunps": "python scripts/build-cunps.py",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.4",
    "vite": "^8.1.0",
    "vite-plugin-pwa": "^1.0.3"
  }
}
